services:
  backend:
    build: ./backend
    env_file:
      - .env
    volumes:
      - product-data:/data
    ports:
      - "${BACKEND_PORT:-8000}:8000"

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "${FRONTEND_PORT:-5173}:80"

volumes:
  product-data:
